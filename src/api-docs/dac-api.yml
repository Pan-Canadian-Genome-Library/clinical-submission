# Description of Health

/dac/{dacId}:
  get:
    summary: Get DAC member information
    responses:
      200:
        description: Returns a 200 OK status code if DAC information is successfully retrieved
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Health'
