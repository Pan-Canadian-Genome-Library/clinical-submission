# @clinical-submission/data-model

Shared types between the submission api and submission ui.

## Overview

This package defines shared types between the submission api and submission ui.

## Installation

This is an internal monorepo package. It's automatically available to other packages in the workspace when adding `@clinical-submission/data-model` as a dependency.
